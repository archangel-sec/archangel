[package]
name = "archangel-config-guide"
version = "0.4.0"
edition = "2021"
description = "Generates the archangel configuration guide as a .docx (no pandoc needed)."
license = "Apache-2.0"
publish = false

[[bin]]
name = "gen-guide"
path = "src/main.rs"

[dependencies]
docx-rs = "0.4"
